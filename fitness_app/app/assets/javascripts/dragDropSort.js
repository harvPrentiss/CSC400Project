$(document).ready(function(){	

	// Makes the lists to add and remove exercises from routines sortable, drag and droppable.
	$('.userExerciseList').sortable({
		connectWith: '.routineExerciseList',
		placeholder: 'placeholder',
	});



});)