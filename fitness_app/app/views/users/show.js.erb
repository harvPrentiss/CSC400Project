<% if @statuses.all.count>0 %>
$('ul').append('<%= escape_javascript(render(partial: '@statuses')) %>');
$('a.hook').attr('href', '<%= statuses_path(page: (params[:page].to_i || 1)+1, per_page: params[:per_page] || 15) %>').show();
<% else %>
$('a.hook').hide();
<% end %>