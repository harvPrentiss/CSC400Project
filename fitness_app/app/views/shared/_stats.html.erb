<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>" class="followStats">
    <strong id="following">
      <%= @user.followed_users.count %>
    </strong>
    Buddies Followed
  </a>
  <br>
  <a href="<%= followers_user_path(@user) %>" class="followStats">
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    Buddies Following
  </a>
</div>